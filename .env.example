# Optional fallback for a single-shop deployment. Owner-issued activation links
# also carry the endpoint automatically, so mechanics never enter this value.
VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
